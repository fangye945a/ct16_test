# 控制器网关管理平台 - 需求拆解文档

## 产品概述

- **产品类型**: 工业物联网网关设备 Web 管理后台
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 工业物联网运维工程师、系统集成工程师、设备管理员
- **核心价值**: 提供网关设备的一站式运维管理平台，涵盖系统监控、拓扑可视化、文件日志管理、OTA 升级、可视化编程及端侧智能体配置
- **界面语言**: 中文
- **主题偏好**: 深色（工业科技感，深蓝/青色主色调，科技蓝/绿色强调色）
- **导航模式**: 路径导航
- **导航布局**: Sidebar（内部运维工具/中后台管理系统）

---

## 页面结构总览

> **说明**：此表为页面生成的唯一数据源，包含所有页面（一级+二级）

| 页面名称 | 文件名 | 路由 | 页面类型 | 入口来源 |
|---------|-------|------|---------|---------|
| 系统概览 | `DashboardPage.tsx` | `/` | 一级 | 导航 |
| 系统拓扑图 | `TopologyPage.tsx` | `/topology` | 一级 | 导航 |
| 文件管理 | `FileManagerPage.tsx` | `/files` | 一级 | 导航 |
| 日志管理 | `LogViewerPage.tsx` | `/logs` | 一级 | 导航 |
| OTA 升级 | `OtaUpgradePage.tsx` | `/ota` | 一级 | 导航 |
| Node-RED 编程 | `NodeRedPage.tsx` | `/nodered` | 一级 | 导航 |
| 智能体配置 | `PicoClawPage.tsx` | `/picoclaw` | 一级 | 导航 |
| 系统设置 | `SettingsPage.tsx` | `/settings` | 一级 | 导航 |
| 节点详情（拓扑图） | `NodeDetailDrawer.tsx` | 无路由（抽屉组件） | 二级 | 拓扑图页 → 点击节点 |
| 日志预览 | `LogPreviewDialog.tsx` | 无路由（弹窗组件） | 二级 | 日志管理页 → 点击日志文件 |
| 技能详情配置 | `SkillConfigDialog.tsx` | 无路由（弹窗组件） | 二级 | 智能体配置页 → 点击技能 |

---

## 页面布局建议

- **系统概览页**: 控制台布局 — 顶部状态栏 + KPI 卡片行 + 图表网格区；视觉重心在实时指标卡片和趋势图表；结果承载区为图表卡片网格，初始态为带骨架屏的 mock 数据
- **拓扑图页**: 全屏画布布局 — 顶部工具栏 + 中央可缩放拖拽画布；视觉重心在拓扑节点和连线；结果承载区为全屏 SVG/Canvas 画布，初始态为预置的 mock 拓扑结构
- **文件管理页**: 左右分栏 — 左侧树形目录 + 右侧文件列表/预览；视觉重心在右侧文件列表；结果承载区为文件表格，初始态为 mock 目录树和文件列表
- **日志管理页**: 上下分区 — 上方筛选栏 + 下方日志表格/预览；视觉重心在日志表格；结果承载区为日志表格 + 底部预览面板，初始态为 mock 日志数据
- **OTA 升级页**: 上下分区 — 上方当前版本信息 + 下方升级操作区/历史记录；视觉重心在上传区和进度条；结果承载区为进度展示区 + 历史记录表格，初始态为当前版本信息
- **Node-RED 编程页**: 三栏布局 — 左侧节点面板 + 中间流程画布 + 右侧信息/调试面板；视觉重心在中间画布；结果承载区为流程画布，初始态为预置示例流程
- **智能体配置页**: 上下分区 — 上方智能体参数配置 + 下方技能列表/市场；视觉重心在技能卡片网格；结果承载区为技能卡片 + 运行统计图表，初始态为 mock 技能数据
- **系统设置页**: 单栏表单布局 — 分组设置表单；视觉重心在表单控件；结果承载区为保存反馈 toast，初始态为 mock 当前配置值

---

## 导航配置

- **导航布局**: Sidebar（左侧固定导航栏）
- **导航项**（仅一级页面）:

| 导航文字 | 路由 | 图标(可选) |
|---------|------|-----------|
| 系统概览 | `/` | Dashboard |
| 拓扑图 | `/topology` | Share2 |
| 文件管理 | `/files` | FolderTree |
| 日志管理 | `/logs` | ScrollText |
| OTA 升级 | `/ota` | Upload |
| Node-RED 编程 | `/nodered` | Workflow |
| 智能体配置 | `/picoclaw` | Bot |
| 系统设置 | `/settings` | Settings |

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 系统概览指标（CPU/内存/磁盘/网络/运行时间等） | demo-mock | 从 `src/data/dashboard.ts` 读取静态 mock 数据，模拟实时数据通过定时器微调数值 | ✅ 本身就是 mock |
| 拓扑图节点与连线数据 | demo-mock | 从 `src/data/topology.ts` 读取预置的网关子模块拓扑结构（节点列表 + 连线列表） | ✅ 本身就是 mock |
| 文件系统目录树与文件列表 | demo-mock | 从 `src/data/files.ts` 读取 mock 目录结构和文件列表 | ✅ 本身就是 mock |
| 日志文件列表与日志内容 | demo-mock | 从 `src/data/logs.ts` 读取 mock 日志数据，含时间戳、级别、来源、内容 | ✅ 本身就是 mock |
| 日志导出（下载） | import-export | 将选中的日志数据序列化为文本文件，通过 Blob + a.click 触发下载 | 无 |
| OTA 升级包上传 | demo-mock | 模拟上传流程：拖拽/选择文件 → 校验 → 模拟进度条推进 → 完成 | ✅ 本身就是 mock（模拟上传行为） |
| OTA 升级历史记录 | demo-mock | 从 `src/data/ota.ts` 读取 mock 升级历史列表 | ✅ 本身就是 mock |
| Node-RED 流程数据 | demo-mock | 从 `src/data/nodered.ts` 读取预置的示例流程（节点 + 连线），画布渲染为静态展示 | ✅ 本身就是 mock |
| 智能体技能列表 | demo-mock | 从 `src/data/picoclaw.ts` 读取 mock 技能数据（名称、版本、状态、描述、参数） | ✅ 本身就是 mock |
| 智能体运行统计（调用次数/成功率/响应时间） | demo-mock | 从 `src/data/picoclaw.ts` 读取 mock 统计数据 | ✅ 本身就是 mock |
| 系统设置配置值 | demo-mock | 从 `src/data/settings.ts` 读取 mock 当前配置，表单修改仅本地 state 变更 + toast 反馈 | ✅ 本身就是 mock |

---

## 功能列表

> **说明**：每个页面/区块的功能点，供页面生成使用

- **全局布局**:
  - **布局目标**: 提供统一的深色工业风格框架，含顶部状态栏和左侧导航
  - **功能点**:
    - **顶部状态栏**: 展示设备名称、运行状态指示灯（绿/黄/红）、当前时间、用户信息（mock 用户名"Admin"）
    - **左侧导航栏**: 8 个导航项，当前页面高亮，带图标和文字，支持折叠
    - **全局主题**: 深色背景（`#0a0e17` 系），主色调深蓝/青色，强调色科技蓝（`#00d4ff`）/绿色（`#00ff88`），卡片背景半透明深色

- **页面: 系统概览** (`/`)
  - **页面目标**: 提供网关设备核心运行指标的一目了然总览
  - **功能点**:
    - **设备信息卡片**: 展示设备名称、型号、序列号、固件版本，静态信息
    - **运行时间展示**: 实时计时器显示自上次启动以来的运行时长（天/时/分/秒）
    - **实时指标卡片行**（4 个）: CPU 使用率、内存使用率、磁盘使用率、网络流量，每个卡片含环形进度条/数值 + 趋势箭头（↑/↓）+ 变化百分比
    - **系统状态总览**: 顶部醒目位置展示整体状态标识（正常=绿色脉冲动画 / 警告=黄色 / 异常=红色闪烁），综合各指标判定
    - **趋势迷你图**: 每个指标卡片内含 Sparkline 折线图，展示近 10 个采样点的趋势
    - **指标模拟更新**: 通过 `setInterval` 每 3 秒微调 mock 数值（±2% 随机波动），模拟实时数据变化

- **页面: 系统拓扑图** (`/topology`)
  - **页面目标**: 可视化展示网关内部各子模块的拓扑结构和实时状态
  - **功能点**:
    - **拓扑画布**: 使用 SVG/Canvas 渲染节点和连线，支持鼠标滚轮缩放、拖拽平移画布
    - **节点渲染**: 6 类节点（主控制器、通信模块、协议转换、边缘计算、安全模块、存储模块），每类不同图标和颜色标识；节点显示名称 + 实时状态圆点（绿在线/灰离线/红故障）
    - **连线渲染**: 节点间带箭头连线表示数据流向，线宽/颜色可表示流量大小或状态
    - **节点详情抽屉**: 点击节点弹出右侧 Drawer，展示节点详细信息（类型、状态、IP 地址、固件版本、运行时长、实时吞吐量等 mock 数据）
    - **工具栏**: 顶部含缩放按钮（+/-/适应画布）、图例说明、刷新按钮
    - **状态模拟**: 节点状态随机分布（大部分在线，1-2 个离线或故障），每 10 秒随机切换一个节点状态模拟动态变化

- **页面: 文件管理** (`/files`)
  - **页面目标**: 浏览网关文件系统目录结构，管理文件和文件夹
  - **功能点**:
    - **树形目录**: 左侧可折叠树形结构展示文件系统目录（`/root` → `/data`, `/logs`, `/config`, `/scripts` 等 mock 目录）
    - **文件列表**: 右侧表格展示当前目录下的文件和子目录，列含名称、类型、大小、修改时间；支持点击进入子目录或返回上级
    - **文件搜索**: 顶部搜索框，输入关键词实时过滤当前目录下的文件和文件夹名称
    - **面包屑导航**: 顶部显示当前路径的面包屑，支持点击跳转
    - **文件操作**: 行操作按钮（预览文本文件、下载），文件夹操作（展开/折叠）

- **页面: 日志管理** (`/logs`)
  - **页面目标**: 查看、筛选和导出网关系统日志
  - **功能点**:
    - **日志级别筛选**: 顶部 Tab/标签切换（全部 / INFO / WARN / ERROR / DEBUG），点击过滤对应级别日志
    - **日志列表**: 表格展示日志条目，列含时间戳、级别（带颜色 Badge）、来源模块、日志内容摘要；按时间倒序排列
    - **日志预览弹窗**: 点击日志行弹出 Dialog，展示完整日志内容（含堆栈信息等 mock 详细内容）
    - **日志导出**: 顶部"导出"按钮，点击将当前筛选后的日志导出为 `.log` 文本文件下载
    - **批量导出**: 支持勾选多条日志行，批量导出选中日志为单个文件
    - **时间排序**: 默认按时间倒序，支持点击列头切换正序/倒序

- **页面: OTA 升级** (`/ota`)
  - **页面目标**: 管理网关固件的在线升级流程
  - **功能点**:
    - **当前版本信息**: 顶部卡片展示当前固件版本、构建日期、分区信息（主分区/备用分区标识）
    - **升级包上传**: 拖拽上传区域，支持拖拽 `.bin` / `.img` 文件或点击选择文件；上传后显示文件名、大小
    - **升级包信息展示**: 上传后展示升级包版本号、兼容型号列表、更新日志（mock 数据）
    - **升级前校验**: 上传后自动校验（模拟 2 秒校验过程），显示校验结果（通过/失败）+ 风险提示（如"升级将重启设备，请确保无关键任务运行"）
    - **升级进度**: 点击"开始升级"后展示进度条（模拟 0%→100% 约 10 秒），含阶段文字（"正在备份当前分区..." → "正在写入新固件..." → "校验中..." → "完成"）
    - **双分区说明**: 升级区域旁展示双分区升级原理说明（图标+文字，说明主备分区切换机制）
    - **升级回滚**: 升级完成后展示"回滚"按钮（模拟功能，点击后弹出确认 + 模拟回滚进度）
    - **升级历史**: 底部表格展示历史升级记录（版本号、升级时间、状态成功/失败/回滚、操作人 mock 数据）

- **页面: Node-RED 编程** (`/nodered`)
  - **页面目标**: 提供嵌入式可视化流程编程界面，构建自动化数据处理流
  - **功能点**:
    - **左侧节点面板**: 按分类折叠的节点库（输入、输出、逻辑、数据转换、网络、工业协议），每类含 3-5 个 mock 节点；节点可拖拽到画布
    - **中间流程画布**: 网格背景画布，支持拖拽节点放置、节点间连线（从输出端拖到输入端）；展示预置的示例流程（如"Modbus 数据采集 → 数据转换 → MQTT 发布"）
    - **右侧信息/调试面板**: 选中节点时显示属性配置表单（mock 字段如名称、Topic、地址等）；底部调试输出区展示 mock 调试日志
    - **顶部工具栏**: 部署按钮（模拟部署成功 toast）、导入/导出按钮（模拟 JSON 导入导出）、流程管理下拉（新建/保存/切换流程）
    - **画布交互**: 支持画布缩放（滚轮）、平移（拖拽空白区域）、节点选中高亮、连线删除（右键/快捷键）
    - **示例流程**: 预置 2-3 个典型物联网数据流示例流程（如"传感器数据采集→边缘过滤→云端上报"）

- **页面: 智能体配置** (`/picoclaw`)
  - **页面目标**: 管理 PicoClaw 端侧智能体的技能和运行参数
  - **功能点**:
    - **智能体参数配置**: 顶部配置区，含模型选择下拉（mock 选项：TinyLLM-1B / MiniGPT-3B）、上下文长度滑块（512-4096）、温度参数滑块（0-2）、最大 Token 数输入
    - **已安装技能列表**: 卡片网格展示已安装技能，每张卡片含技能名称、版本号、状态开关（启用/停用）、简短描述；点击卡片进入技能详情配置弹窗
    - **技能详情配置弹窗**: Dialog 展示技能独立参数（mock 示例：数据过滤技能的"阈值"、"过滤规则"等表单字段），支持修改并保存
    - **技能市场**: 底部 Tab 切换至"技能市场"，展示可添加的技能列表（mock 5-8 个技能卡片，含"安装"按钮），点击安装后添加到已安装列表
    - **运行状态监控**: 右侧面板或底部区域展示智能体运行统计：总调用次数、成功率（环形图）、平均响应时间（数值+趋势）、近 24 小时调用量柱状图（mock 数据）
    - **状态模拟**: 技能状态随机分布（大部分启用/正常运行），运行统计数值通过定时器微调模拟实时变化

- **页面: 系统设置** (`/settings`)
  - **页面目标**: 配置网关设备的系统级参数
  - **功能点**:
    - **网络设置**: 表单组含 IP 地址、子网掩码、网关、DNS 服务器（mock 默认值），支持修改并保存（toast 反馈）
    - **时间设置**: 时区选择下拉、NTP 服务器地址输入、手动设置日期时间
    - **安全设置**: 管理员密码修改表单、访问控制（允许的 IP 白名单列表 mock 数据）
    - **系统服务管理**: 各服务模块的启停开关列表（Modbus 服务、MQTT Broker、OPC UA 服务等 mock 服务），带状态指示灯
    - **保存与重置**: 底部"保存配置"按钮（toast 反馈保存成功）、"恢复默认"按钮（弹出确认后重置为 mock 默认值）

---

## 数据共享配置

无跨页面数据共享需求。各页面独立使用各自的 mock 数据源，页面间通过路由切换，无状态依赖。

---

## 全局状态与交互规范

- **状态指示灯颜色编码**（全局统一）:
  - 绿色 (`#00ff88`): 正常/在线/运行中
  - 黄色 (`#ffaa00`): 警告/高负载
  - 红色 (`#ff4444`): 异常/故障/离线
  - 灰色 (`#666666`): 未启用/未知
- **动画规范**: 页面切换使用淡入淡出（opacity transition 200ms）；状态指示灯使用脉冲动画（绿色正常慢脉冲，红色异常快脉冲）；进度条平滑过渡；卡片 hover 微上浮 + 边框发光
- **响应式**: 侧边栏在大屏固定展开，中屏可折叠，小屏（<768px）变为底部 Tab 导航或汉堡菜单

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 无参考材料，从工业物联网网关的产品语义与深色工业风格需求出发自主建立视觉方向
- **核心情绪 / 应用类型**: 工业物联网网关设备管理控制台 —— 精密、可靠、实时监控、工程可信感
- **独特记忆点**: 终端仪表舱 —— 深色底板上用青色扫描线与绿色状态脉冲构建"设备机柜监视窗"的视觉母题，每个数据卡片像一块嵌入式仪表盘

## 2. Art Direction

- **方向名**: Industrial Terminal Dark
- **Design Style**: Minimal Dark 极简深色 + Terminal 终端感 —— 工业网关需要低亮度长时间监控、高对比数据聚焦、设备状态的即时可读性；终端感来自等宽数据标注、扫描线装饰和状态脉冲动画
- **DNA 参数**: 圆角 subtle（rounded-md，面板与卡片保持轻微工业倒角，不圆润）/ 阴影 subtle（shadow-sm，深色底上阴影仅用于层级抬升，不厚重）/ 间距 standard（gap-4 p-6，信息密集但不拥挤）/ 字体方向 等宽数据 + 无衬线正文 / 装饰手法 细线分隔、状态脉冲点、扫描线纹理、数据标注的等宽数字
- **应用类型**: Tool / Dashboard —— 左侧导航 + 顶部状态栏 + 内容区，信息密度中高，以卡片网格和拓扑画布为主

## 3. Color System

**色彩关系**: 深蓝黑基底 + 青色主识别 + 翠绿状态脉冲 + 低饱和灰蓝辅助面
**配色设计理由**: primary 青色用于主交互、当前导航、数据高亮和品牌锚点，在深色底上保持高可读性与科技感；bg 深蓝黑模拟机柜内部暗环境，降低长时间监控的视觉疲劳；accent 灰蓝用于卡片、菜单 hover 和选中态，与 primary 形成冷暖微对比但不抢主色权重；状态绿/黄/红在深色底上保持工业信号灯语义
**主色推导**: 工业物联网网关的"科技蓝/青色"需求 + 深色监控环境 → primary 取青蓝 `hsl(190 85% 52%)`，既满足"科技蓝"又带有设备仪表感的冷调；强调绿用于正常状态和成功反馈，与青色同属冷色系，色温一致
**使用比例**: 60% 深蓝黑中性底 / 30% 灰蓝辅助面 / 10% 青色 primary

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(210 25% 6%) | 深蓝黑基底，模拟机柜暗环境 |
| card | `--card` | `bg-card` | hsl(210 20% 10%) | 卡片与面板，比 bg 微亮形成层级 |
| text | `--foreground` | `text-foreground` | hsl(200 10% 90%) | 标题和正文，高对比可读 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(210 8% 55%) | 占位符、说明、辅助元信息 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(190 85% 52%) | 主交互、CTA、导航激活、数据高亮 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(210 25% 6%) | primary 上的深色文字 |
| accent | `--accent` | `bg-accent` | hsl(210 15% 16%) | hover/focus 浅底、选中态、Skeleton |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(200 10% 85%) | accent 上的文字和图标 |
| border | `--border` | `border-border` | hsl(210 12% 22%) | 输入框、卡片、菜单边界，细线分隔 |

**语义色提示**:
- 成功/正常: bg `hsl(160 70% 18%)` / border `hsl(160 65% 38%)` / text `hsl(160 80% 55%)` — 翠绿色温与 primary 青色同为冷色系，饱和度对齐
- 警告: bg `hsl(40 70% 16%)` / border `hsl(40 65% 42%)` / text `hsl(40 90% 58%)` — 暖黄与冷色主调形成对比但不刺眼
- 错误/异常: bg `hsl(0 60% 16%)` / border `hsl(0 55% 40%)` / text `hsl(0 75% 58%)` — 红色仅用于故障状态，不用于装饰
- 离线/灰色: bg `hsl(210 5% 18%)` / border `hsl(210 5% 28%)` / text `hsl(210 5% 50%)` — 低饱和灰，表示非活跃

## 4. 字体与节奏

- **font-display**: Space Grotesk —— 几何无衬线，带有轻微机械感和科技气质，适合工业设备标题与数据卡片
- **font-body**: Noto Sans SC —— 中文正文清晰可读，与 Space Grotesk 的几何感协调，不引入额外装饰
- **字号**: H1 text-3xl ~ text-4xl；H2 text-xl ~ text-2xl；body text-sm ~ text-base；数据标注 text-xs ~ text-sm 等宽
- **圆角**: subtle — `rounded-md`（卡片、按钮、输入框），工业设备面板的轻微倒角，不圆润不锋利

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导 —— 左侧导航 + 顶部状态栏 + 右侧内容区
- **Page / Section Order**: 系统概览 → 拓扑图 → 文件管理 → 日志导出 → OTA升级 → Node-RED编程 → 智能体配置 → 系统设置
- **Standard Content Zone**: `max-w-[1400px] mx-auto`，适配仪表盘卡片网格和拓扑图画布
- **Shell / Frame Alignment**: 内容区与左侧导航独立滚动，内容区自身使用标准内容区约束
- **Padding & Rhythm**: `px-6 lg:px-8 py-6`，卡片内 `p-5`，间距 `gap-4` 至 `gap-6`，保持 4px 倍数节奏
- **Full-bleed Zones**: 拓扑图画布可突破内容区宽度至 `w-full`，工具栏和节点面板保持在内容区内；Node-RED 流程画布全宽
- **Local Narrowing**: 表单、设置页、智能体配置参数区收窄至 `max-w-2xl`；日志预览使用 `max-w-4xl`
- **Overflow Strategy**: 拓扑图画布使用 `overflow-hidden` + 内部缩放拖拽；宽表格、日志列表使用 `overflow-x-auto`
- **Flexibility Boundary**: 允许移动端调整卡片列数和 padding；全局 max-w、圆角系统、主色和阴影语言保持一致

## 6. 视觉与动效

- **装饰**: 细线分隔 + 状态脉冲点 + 等宽数据标注；卡片顶部可选 1px 青色顶部描边作为活跃指示
- **阴影/边界**: 轻 —— `shadow-sm` 用于卡片抬升，`border-border` 细线分隔为主
- **动效**: 精致 —— 状态脉冲点使用 `animate-pulse` 呼吸（正常绿 2s、警告黄 1s、错误红 0.5s）；页面切换 150ms fade；hover 时卡片 border 变亮 100ms；拓扑图节点连线使用虚线流动动画表示数据方向；数字变化使用轻量 transition

## 7. 组件原则

- 按钮、表单、菜单、卡片必须有 Default / Hover / Active / Focus / Disabled 状态
- Primary 按钮使用 `bg-primary text-primary-foreground`，承担主行动（部署、升级、保存）；Secondary 使用 `border-border` 描边 + `bg-card`；Ghost 使用 `accent` 承接 hover
- 状态指示器（在线/离线/故障）使用语义色圆点 + 文字，圆点带 pulse 动画
- 拓扑图节点使用圆角矩形卡片，状态色左边框 3px，类型图标居中，连线使用 SVG path 虚线
- 加载与空状态延续深色工业风格，Skeleton 使用 `accent` 底色，空状态使用 muted 文字 + 工业图标

## 8. Image Direction

- **Image Role**: 无强制图片需求，优先通过排版、色彩和局部图形（状态脉冲、扫描线纹理、拓扑连线）建立视觉记忆点
- **Image Art Direction**: 若需品牌主视觉或登录页背景，使用深色工业场景——服务器机柜 LED 指示灯矩阵、电路板铜色走线微距、工厂车间暗光下的设备指示灯，构图以重复几何和点光源为主，光线冷调偏青，材质为金属/玻璃/PCB
- **Image Prompt Keywords**: dark industrial server room, LED indicator matrix close-up, circuit board copper traces macro, cyan and green point lights, shallow depth of field, matte metal surface, low ambient light, geometric repetition, industrial IoT aesthetic
- **Image Avoidance**: 避免通用科技感蓝色电路板素材图、商务人物操作平板素材、无主题抽象渐变背景、过曝的高光渲染

## 9. Anti-patterns

- **Split personality**: 拓扑图页使用亮色画布或白色节点卡片，破坏深色工业统一感；全站保持深蓝黑基底
- **Phantom tokens**: 编造 `--industrial-glow` 等不存在变量；所有视觉 token 在 9 角色色板内
- **Default SaaS drift**: 回到默认蓝 `hsl(221 83% 53%)` 按钮或通用紫渐变卡片；用青色 primary 和深蓝黑 bg 保持工业终端感
- **Invisible interaction**: 拓扑图节点 hover 无反馈、拖拽无视觉变化；每个可交互节点必须有 hover 亮边和 cursor 变化
- **Mono-hue tyranny**: 青色铺满导航激活、按钮、链接、图标、图表；primary 收回到 CTA 和当前导航，其余用 accent 和中性色
- **Status color drift**: 状态绿饱和度过高变成荧光绿，与克制的青色 primary 脱节；语义色饱和度与 primary 对齐在 50-85% 区间
- **Typography chaos**: 数据卡片混用衬线体或手写体；数据标注统一使用等宽数字，正文使用 Noto Sans SC